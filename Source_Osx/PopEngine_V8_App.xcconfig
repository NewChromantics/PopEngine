#include "PopEngine.xcconfig"

GCC_PREPROCESSOR_DEFINITIONS = $(GCC_PREPROCESSOR_DEFINITIONS) JSAPI_V8

V8_PATH = $(SRCROOT)/src/v8

HEADER_SEARCH_PATHS = $(HEADER_SEARCH_PATHS) $(V8_PATH) $(V8_PATH)/include
LIBRARY_SEARCH_PATHS = $(LIBRARY_SEARCH_PATHS) $(V8_PATH)/lib
